const config = {
  appId: "app.doggstyle.ios",
  appName: "Dogg Style",
  webDir: "dist/client",
  ios: {
    contentInset: "automatic",
    preferredContentMode: "mobile",
    scheme: "Dogg Style",
  },
};

export default config;
